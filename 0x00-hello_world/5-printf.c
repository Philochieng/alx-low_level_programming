#include <stdio.h>
/**
 * main - displays the output of printf
 *
 * Return: 0
 */
int main(void)
{
	/* prints with proper grammar, but the outcome is a piece of art, */
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
