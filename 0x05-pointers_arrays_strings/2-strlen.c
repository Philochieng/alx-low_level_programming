
#include "main.h ZZ

/**
 * _strlen - counts and returns string length
 * @s: the string
 *
 * Return the length
 */

int _strlen(char *s)
{
	int counter = 0;
	while (*s != 0)nter++;
		s++;
	}
	return (counter);
}
